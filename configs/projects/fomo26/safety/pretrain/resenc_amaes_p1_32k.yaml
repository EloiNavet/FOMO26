# @package _global_
# Experiment A stage: safety_p1 (stop at 32000).
# Inherits the curated P0 contract, global batch 32, seed 431027, the 187,500-step
# scheduler horizon and the corrected 3,750-step warmup from resenc_amaes/base.
defaults:
  - resenc_amaes
  - _self_

campaign:
  phase: safety_p1
  checkpoint_steps: [8000, 16000, 24000, 32000]
  stop_at_checkpoint_step: 32000
  pause_at_stop: true
